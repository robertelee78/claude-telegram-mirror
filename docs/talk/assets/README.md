# Talk assets

Drop the two referenced assets here, then re-run `../build.sh`:

- `tui-widget.png` — screenshot of Claude Code's AskUserQuestion multiple-choice
  TUI widget in the terminal (Beat 3).
- `demo.mp4` — ≤90s side-by-side recording of the phone→terminal trivia demo
  (Beat 8; also the on-stage fallback). For embedding a still in the PDF, also
  export a `demo.png` poster frame.

The deck currently references these as text placeholders so the PDF builds
without them; swap the placeholder lines for real `![](assets/…)` images once
captured.
